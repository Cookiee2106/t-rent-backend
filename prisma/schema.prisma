generator client {
  provider        = "prisma-client-js"
  output          = "../node_modules/.prisma/client"
  previewFeatures = ["partialIndexes"]
}

datasource db {
  provider = "postgresql"
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model bo_di_kem {
  id                                                              String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  mau_thiet_bi_id                                                 String                 @db.Uuid
  ten_thanh_phan                                                  String                 @db.VarChar(255)
  so_luong                                                        Int                    @default(1)
  loai_quan_ly                                                    String                 @db.VarChar(50)
  phu_kien_id                                                     String?                @db.Uuid
  mau_thiet_bi_thanh_phan_id                                      String?                @db.Uuid
  bat_buoc                                                        Boolean                @default(true)
  ghi_chu                                                         String?
  created_at                                                      DateTime               @default(now()) @db.Timestamptz(6)
  mau_thiet_bi_bo_di_kem_mau_thiet_bi_idTomau_thiet_bi            mau_thiet_bi           @relation("bo_di_kem_mau_thiet_bi_idTomau_thiet_bi", fields: [mau_thiet_bi_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  mau_thiet_bi_bo_di_kem_mau_thiet_bi_thanh_phan_idTomau_thiet_bi mau_thiet_bi?          @relation("bo_di_kem_mau_thiet_bi_thanh_phan_idTomau_thiet_bi", fields: [mau_thiet_bi_thanh_phan_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phu_kien                                                        phu_kien?              @relation(fields: [phu_kien_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phu_kien_gan_voi_don                                            phu_kien_gan_voi_don[]
  thiet_bi_gan_voi_don                                            thiet_bi_gan_voi_don[]

  @@index([mau_thiet_bi_id], map: "idx_bo_di_kem_mau")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model chi_tiet_ban_giao {
  id                      String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  phieu_ban_giao_id       String                @db.Uuid
  thiet_bi_gan_voi_don_id String?               @db.Uuid
  phu_kien_gan_voi_don_id String?               @db.Uuid
  so_luong_giao           Int                   @default(1)
  tinh_trang              String?
  ghi_chu                 String?
  created_at              DateTime              @default(now()) @db.Timestamptz(6)
  phieu_ban_giao          phieu_ban_giao        @relation(fields: [phieu_ban_giao_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phu_kien_gan_voi_don    phu_kien_gan_voi_don? @relation(fields: [phu_kien_gan_voi_don_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  thiet_bi_gan_voi_don    thiet_bi_gan_voi_don? @relation(fields: [thiet_bi_gan_voi_don_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([phieu_ban_giao_id], map: "idx_chi_tiet_ban_giao_phieu")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model chi_tiet_don_thue {
  id                     String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  don_thue_id            String                 @db.Uuid
  mau_thiet_bi_id        String                 @db.Uuid
  so_luong               Int                    @default(1)
  gia_thue_ngay_snapshot Decimal                @default(0) @db.Decimal(12, 2)
  tien_coc_snapshot      Decimal                @default(0) @db.Decimal(12, 2)
  tien_thue              Decimal                @default(0) @db.Decimal(12, 2)
  tien_coc               Decimal                @default(0) @db.Decimal(12, 2)
  trang_thai             String                 @default("CHO_CHUAN_BI") @db.VarChar(50)
  created_at             DateTime               @default(now()) @db.Timestamptz(6)
  updated_at             DateTime               @default(now()) @db.Timestamptz(6)
  don_thue               don_thue               @relation(fields: [don_thue_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  mau_thiet_bi           mau_thiet_bi           @relation(fields: [mau_thiet_bi_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phu_kien_gan_voi_don   phu_kien_gan_voi_don[]
  thiet_bi_gan_voi_don   thiet_bi_gan_voi_don[]

  @@index([don_thue_id], map: "idx_chi_tiet_don_thue_don")
  @@index([mau_thiet_bi_id], map: "idx_chi_tiet_don_thue_mau")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model chi_tiet_gio_hang {
  id                        String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  gio_hang_id               String                      @db.Uuid
  mau_thiet_bi_id           String                      @db.Uuid
  so_luong                  Int                         @default(1)
  ngay_nhan                 DateTime                    @db.Timestamptz(6)
  ngay_tra                  DateTime                    @db.Timestamptz(6)
  gia_thue_ngay_snapshot    Decimal                     @default(0) @db.Decimal(12, 2)
  tien_coc_snapshot         Decimal                     @default(0) @db.Decimal(12, 2)
  trang_thai                String                      @default("HOAT_DONG") @db.VarChar(50)
  created_at                DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at                DateTime                    @default(now()) @db.Timestamptz(6)
  gio_hang                  gio_hang                    @relation(fields: [gio_hang_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  mau_thiet_bi              mau_thiet_bi                @relation(fields: [mau_thiet_bi_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  chi_tiet_phien_thanh_toan chi_tiet_phien_thanh_toan[]

  @@index([gio_hang_id], map: "idx_chi_tiet_gio_hang_gio")
  @@index([mau_thiet_bi_id], map: "idx_chi_tiet_gio_hang_mau")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model chi_tiet_phien_thanh_toan {
  id                     String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  phien_thanh_toan_id    String             @db.Uuid
  chi_tiet_gio_hang_id   String?            @db.Uuid
  mau_thiet_bi_id        String             @db.Uuid
  so_luong               Int                @default(1)
  ngay_nhan              DateTime           @db.Timestamptz(6)
  ngay_tra               DateTime           @db.Timestamptz(6)
  gia_thue_ngay_snapshot Decimal            @default(0) @db.Decimal(12, 2)
  tien_coc_snapshot      Decimal            @default(0) @db.Decimal(12, 2)
  tien_thue              Decimal            @default(0) @db.Decimal(12, 2)
  tien_coc               Decimal            @default(0) @db.Decimal(12, 2)
  created_at             DateTime           @default(now()) @db.Timestamptz(6)
  chi_tiet_gio_hang      chi_tiet_gio_hang? @relation(fields: [chi_tiet_gio_hang_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  mau_thiet_bi           mau_thiet_bi       @relation(fields: [mau_thiet_bi_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phien_thanh_toan       phien_thanh_toan   @relation(fields: [phien_thanh_toan_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([phien_thanh_toan_id], map: "idx_chi_tiet_phien_thanh_toan_phien")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model chi_tiet_tra_thiet_bi {
  id                      String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  phieu_tra_id            String                @db.Uuid
  thiet_bi_gan_voi_don_id String?               @db.Uuid
  phu_kien_gan_voi_don_id String?               @db.Uuid
  so_luong_tra            Int                   @default(0)
  so_luong_hu             Int                   @default(0)
  so_luong_mat            Int                   @default(0)
  tinh_trang              String?
  ghi_chu                 String?
  created_at              DateTime              @default(now()) @db.Timestamptz(6)
  phieu_tra_thiet_bi      phieu_tra_thiet_bi    @relation(fields: [phieu_tra_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phu_kien_gan_voi_don    phu_kien_gan_voi_don? @relation(fields: [phu_kien_gan_voi_don_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  thiet_bi_gan_voi_don    thiet_bi_gan_voi_don? @relation(fields: [thiet_bi_gan_voi_don_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phi_phat_sinh           phi_phat_sinh[]

  @@index([phieu_tra_id], map: "idx_chi_tiet_tra_thiet_bi_phieu")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model danh_muc_thiet_bi {
  id           String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ten_danh_muc String         @unique @db.VarChar(100)
  trang_thai   String         @default("HOAT_DONG") @db.VarChar(50)
  created_at   DateTime       @default(now()) @db.Timestamptz(6)
  mau_thiet_bi mau_thiet_bi[]

  @@index([trang_thai], map: "idx_danh_muc_thiet_bi_trang_thai")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model dieu_khoan_thue {
  id                  String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  phien_ban           String                @db.VarChar(50)
  noi_dung            String
  ma_bam_noi_dung     String?               @db.VarChar(255)
  trang_thai          String                @default("HOAT_DONG") @db.VarChar(50)
  nguoi_tao_id        String?               @db.Uuid
  created_at          DateTime              @default(now()) @db.Timestamptz(6)
  nguoi_dung          nguoi_dung?           @relation(fields: [nguoi_tao_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  xac_nhan_dieu_khoan xac_nhan_dieu_khoan[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model don_thue {
  id                         String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ma_don                     String                       @unique @db.VarChar(100)
  khach_hang_id              String                       @db.Uuid
  ngay_nhan                  DateTime                     @db.Timestamptz(6)
  ngay_tra                   DateTime                     @db.Timestamptz(6)
  so_ngay_thue               Int
  tong_tien_thue             Decimal                      @default(0) @db.Decimal(12, 2)
  tong_tien_coc              Decimal                      @default(0) @db.Decimal(12, 2)
  trang_thai                 String                       @default("DA_GIU_CHO") @db.VarChar(50)
  huy_luc                    DateTime?                    @db.Timestamptz(6)
  ly_do_huy                  String?
  created_at                 DateTime                     @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime                     @default(now()) @db.Timestamptz(6)
  chi_tiet_don_thue          chi_tiet_don_thue[]
  ho_so_khach_hang           ho_so_khach_hang             @relation(fields: [khach_hang_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  hop_dong_giay              hop_dong_giay?
  lich_su_di_chuyen_thiet_bi lich_su_di_chuyen_thiet_bi[]
  lich_su_ton_kho            lich_su_ton_kho[]
  phieu_ban_giao             phieu_ban_giao?
  phieu_bao_tri              phieu_bao_tri[]
  phieu_tra_thiet_bi         phieu_tra_thiet_bi?
  phu_kien_gan_voi_don       phu_kien_gan_voi_don[]
  tep_don_thue               tep_don_thue[]
  thanh_toan                 thanh_toan[]
  thiet_bi_gan_voi_don       thiet_bi_gan_voi_don[]
  xac_nhan_dieu_khoan        xac_nhan_dieu_khoan[]

  @@index([khach_hang_id], map: "idx_don_thue_khach")
  @@index([ngay_nhan, ngay_tra], map: "idx_don_thue_ngay")
  @@index([trang_thai], map: "idx_don_thue_trang_thai")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model gio_hang {
  id                String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  khach_hang_id     String              @db.Uuid
  trang_thai        String              @default("HOAT_DONG") @db.VarChar(50)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  updated_at        DateTime            @default(now()) @db.Timestamptz(6)
  chi_tiet_gio_hang chi_tiet_gio_hang[]
  ho_so_khach_hang  ho_so_khach_hang    @relation(fields: [khach_hang_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phien_thanh_toan  phien_thanh_toan[]

  @@index([khach_hang_id], map: "idx_gio_hang_khach")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model hang_thiet_bi {
  id           String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ten_hang     String         @unique @db.VarChar(100)
  trang_thai   String         @default("HOAT_DONG") @db.VarChar(50)
  created_at   DateTime       @default(now()) @db.Timestamptz(6)
  mau_thiet_bi mau_thiet_bi[]

  @@index([trang_thai], map: "idx_hang_thiet_bi_trang_thai")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model ho_so_khach_hang {
  id                  String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nguoi_dung_id       String                @unique @db.Uuid
  dia_chi             String?
  so_cccd             String?               @unique @db.VarChar(50)
  trang_thai_xac_minh String                @default("CHUA_XAC_MINH") @db.VarChar(50)
  created_at          DateTime              @default(now()) @db.Timestamptz(6)
  updated_at          DateTime              @default(now()) @db.Timestamptz(6)
  don_thue            don_thue[]
  gio_hang            gio_hang[]
  nguoi_dung          nguoi_dung            @relation(fields: [nguoi_dung_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  ho_so_xac_minh      ho_so_xac_minh[]
  hop_dong_giay       hop_dong_giay[]
  phien_thanh_toan    phien_thanh_toan[]
  xac_nhan_dieu_khoan xac_nhan_dieu_khoan[]

  @@index([nguoi_dung_id], map: "idx_ho_so_khach_hang_nguoi_dung")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model ho_so_xac_minh {
  id                String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  khach_hang_id     String           @db.Uuid
  so_cccd           String           @db.VarChar(50)
  anh_mat_truoc_url String?
  anh_mat_sau_url   String?
  trang_thai        String           @default("CHO_DUYET") @db.VarChar(50)
  ly_do_tu_choi     String?
  nguoi_duyet_id    String?          @db.Uuid
  duyet_luc         DateTime?        @db.Timestamptz(6)
  created_at        DateTime         @default(now()) @db.Timestamptz(6)
  ho_so_khach_hang  ho_so_khach_hang @relation(fields: [khach_hang_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  nguoi_dung        nguoi_dung?      @relation(fields: [nguoi_duyet_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([khach_hang_id], map: "idx_ho_so_xac_minh_khach")
  @@index([trang_thai], map: "idx_ho_so_xac_minh_trang_thai")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model hop_dong_giay {
  id               String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ma_hop_dong      String           @unique @db.VarChar(100)
  don_thue_id      String           @unique @db.Uuid
  khach_hang_id    String           @db.Uuid
  nhan_vien_id     String?          @db.Uuid
  ky_luc           DateTime         @default(now()) @db.Timestamptz(6)
  ghi_chu          String?
  created_at       DateTime         @default(now()) @db.Timestamptz(6)
  updated_at       DateTime         @default(now()) @db.Timestamptz(6)
  don_thue         don_thue         @relation(fields: [don_thue_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  ho_so_khach_hang ho_so_khach_hang @relation(fields: [khach_hang_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  nguoi_dung       nguoi_dung?      @relation(fields: [nhan_vien_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tep_hop_dong     tep_hop_dong[]

  @@index([don_thue_id], map: "idx_hop_dong_giay_don")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model lich_su_di_chuyen_thiet_bi {
  id                                                                String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  thiet_bi_id                                                       String          @db.Uuid
  vi_tri_truoc_id                                                   String?         @db.Uuid
  vi_tri_sau_id                                                     String?         @db.Uuid
  trang_thai_truoc                                                  String?         @db.VarChar(50)
  trang_thai_sau                                                    String?         @db.VarChar(50)
  loai_di_chuyen                                                    String          @db.VarChar(50)
  don_thue_id                                                       String?         @db.Uuid
  ghi_chu                                                           String?
  nguoi_thuc_hien_id                                                String?         @db.Uuid
  created_at                                                        DateTime        @default(now()) @db.Timestamptz(6)
  don_thue                                                          don_thue?       @relation(fields: [don_thue_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  nguoi_dung                                                        nguoi_dung?     @relation(fields: [nguoi_thuc_hien_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  thiet_bi_vat_ly                                                   thiet_bi_vat_ly @relation(fields: [thiet_bi_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  vi_tri_kho_lich_su_di_chuyen_thiet_bi_vi_tri_sau_idTovi_tri_kho   vi_tri_kho?     @relation("lich_su_di_chuyen_thiet_bi_vi_tri_sau_idTovi_tri_kho", fields: [vi_tri_sau_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  vi_tri_kho_lich_su_di_chuyen_thiet_bi_vi_tri_truoc_idTovi_tri_kho vi_tri_kho?     @relation("lich_su_di_chuyen_thiet_bi_vi_tri_truoc_idTovi_tri_kho", fields: [vi_tri_truoc_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([don_thue_id], map: "idx_lich_su_di_chuyen_don")
  @@index([thiet_bi_id], map: "idx_lich_su_di_chuyen_thiet_bi")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model lich_su_ton_kho {
  id                 String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  phu_kien_id        String      @db.Uuid
  so_luong_thay_doi  Int
  loai_thay_doi      String      @db.VarChar(50)
  ly_do              String?
  don_thue_id        String?     @db.Uuid
  nguoi_thuc_hien_id String?     @db.Uuid
  created_at         DateTime    @default(now()) @db.Timestamptz(6)
  don_thue           don_thue?   @relation(fields: [don_thue_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  nguoi_dung         nguoi_dung? @relation(fields: [nguoi_thuc_hien_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phu_kien           phu_kien    @relation(fields: [phu_kien_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([don_thue_id], map: "idx_lich_su_ton_kho_don")
  @@index([phu_kien_id], map: "idx_lich_su_ton_kho_phu_kien")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model mau_thiet_bi {
  id                                                           String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  danh_muc_id                                                  String?                     @db.Uuid
  hang_id                                                      String?                     @db.Uuid
  ten_mau                                                      String                      @db.VarChar(255)
  mo_ta                                                        String?
  anh_url                                                      String?
  gia_thue_ngay                                                Decimal                     @default(0) @db.Decimal(12, 2)
  tien_coc                                                     Decimal                     @default(0) @db.Decimal(12, 2)
  trang_thai                                                   String                      @default("HOAT_DONG") @db.VarChar(50)
  da_xoa_luc                                                   DateTime?                   @db.Timestamptz(6)
  created_at                                                   DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at                                                   DateTime                    @default(now()) @db.Timestamptz(6)
  bo_di_kem_bo_di_kem_mau_thiet_bi_idTomau_thiet_bi            bo_di_kem[]                 @relation("bo_di_kem_mau_thiet_bi_idTomau_thiet_bi")
  bo_di_kem_bo_di_kem_mau_thiet_bi_thanh_phan_idTomau_thiet_bi bo_di_kem[]                 @relation("bo_di_kem_mau_thiet_bi_thanh_phan_idTomau_thiet_bi")
  chi_tiet_don_thue                                            chi_tiet_don_thue[]
  chi_tiet_gio_hang                                            chi_tiet_gio_hang[]
  chi_tiet_phien_thanh_toan                                    chi_tiet_phien_thanh_toan[]
  danh_muc_thiet_bi                                            danh_muc_thiet_bi?          @relation(fields: [danh_muc_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  hang_thiet_bi                                                hang_thiet_bi?              @relation(fields: [hang_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  thiet_bi_vat_ly                                              thiet_bi_vat_ly[]

  @@index([danh_muc_id, hang_id], map: "idx_mau_thiet_bi_danh_muc_hang")
  @@index([trang_thai], map: "idx_mau_thiet_bi_trang_thai")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model nguoi_dung {
  id                         String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ho_ten                     String                       @db.VarChar(255)
  email                      String                       @unique @db.VarChar(255)
  so_dien_thoai              String?                      @db.VarChar(20)
  mat_khau_hash              String                       @db.VarChar(255)
  vai_tro                    String                       @default("KHACH_HANG") @db.VarChar(50)
  trang_thai                 String                       @default("HOAT_DONG") @db.VarChar(50)
  da_xoa_luc                 DateTime?                    @db.Timestamptz(6)
  created_at                 DateTime                     @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime                     @default(now()) @db.Timestamptz(6)
  dieu_khoan_thue            dieu_khoan_thue[]
  ho_so_khach_hang           ho_so_khach_hang?
  ho_so_xac_minh             ho_so_xac_minh[]
  hop_dong_giay              hop_dong_giay[]
  lich_su_di_chuyen_thiet_bi lich_su_di_chuyen_thiet_bi[]
  lich_su_ton_kho            lich_su_ton_kho[]
  nhat_ky_thao_tac           nhat_ky_thao_tac[]
  phi_phat_sinh              phi_phat_sinh[]
  phieu_ban_giao             phieu_ban_giao[]
  phieu_bao_tri              phieu_bao_tri[]
  phieu_tra_thiet_bi         phieu_tra_thiet_bi[]
  tep_don_thue               tep_don_thue[]
  tep_hop_dong               tep_hop_dong[]
  xac_thuc_otp               xac_thuc_otp[]

  @@index([email], map: "idx_nguoi_dung_email")
  @@index([vai_tro, trang_thai], map: "idx_nguoi_dung_vai_tro_trang_thai")
}

model nhat_ky_thao_tac {
  id            String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nguoi_dung_id String?     @db.Uuid
  vai_tro       String?     @db.VarChar(50)
  hanh_dong     String      @db.VarChar(100)
  doi_tuong     String?     @db.VarChar(100)
  doi_tuong_id  String?     @db.Uuid
  mo_ta         String?
  du_lieu_truoc Json?
  du_lieu_sau   Json?
  ip_address    String?     @db.VarChar(100)
  user_agent    String?
  created_at    DateTime    @default(now()) @db.Timestamptz(6)
  nguoi_dung    nguoi_dung? @relation(fields: [nguoi_dung_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([created_at], map: "idx_nhat_ky_created_at")
  @@index([doi_tuong, doi_tuong_id], map: "idx_nhat_ky_doi_tuong")
  @@index([nguoi_dung_id], map: "idx_nhat_ky_nguoi_dung")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model phi_phat_sinh {
  id                    String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  phieu_tra_id          String                 @db.Uuid
  chi_tiet_tra_id       String?                @db.Uuid
  loai_phi              String                 @db.VarChar(50)
  mo_ta                 String?
  so_tien               Decimal                @default(0) @db.Decimal(12, 2)
  nguoi_tao_id          String?                @db.Uuid
  created_at            DateTime               @default(now()) @db.Timestamptz(6)
  chi_tiet_tra_thiet_bi chi_tiet_tra_thiet_bi? @relation(fields: [chi_tiet_tra_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  nguoi_dung            nguoi_dung?            @relation(fields: [nguoi_tao_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phieu_tra_thiet_bi    phieu_tra_thiet_bi     @relation(fields: [phieu_tra_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([phieu_tra_id], map: "idx_phi_phat_sinh_phieu")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model phien_thanh_toan {
  id                        String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  khach_hang_id             String                      @db.Uuid
  gio_hang_id               String                      @db.Uuid
  xac_nhan_dieu_khoan_id    String?                     @db.Uuid
  xac_thuc_otp_id           String?                     @db.Uuid
  trang_thai                String                      @default("SAN_SANG_THANH_TOAN") @db.VarChar(50)
  tong_tien_thue            Decimal                     @default(0) @db.Decimal(12, 2)
  tong_tien_coc             Decimal                     @default(0) @db.Decimal(12, 2)
  tong_tien_thanh_toan      Decimal                     @default(0) @db.Decimal(12, 2)
  het_han_luc               DateTime?                   @db.Timestamptz(6)
  da_thanh_toan_luc         DateTime?                   @db.Timestamptz(6)
  that_bai_luc              DateTime?                   @db.Timestamptz(6)
  created_at                DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at                DateTime                    @default(now()) @db.Timestamptz(6)
  chi_tiet_phien_thanh_toan chi_tiet_phien_thanh_toan[]
  gio_hang                  gio_hang                    @relation(fields: [gio_hang_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  ho_so_khach_hang          ho_so_khach_hang            @relation(fields: [khach_hang_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  xac_nhan_dieu_khoan       xac_nhan_dieu_khoan?        @relation(fields: [xac_nhan_dieu_khoan_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  xac_thuc_otp              xac_thuc_otp?               @relation(fields: [xac_thuc_otp_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  thanh_toan                thanh_toan[]

  @@index([gio_hang_id], map: "idx_phien_thanh_toan_gio")
  @@index([khach_hang_id], map: "idx_phien_thanh_toan_khach")
  @@index([trang_thai], map: "idx_phien_thanh_toan_trang_thai")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model phieu_ban_giao {
  id                String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  don_thue_id       String              @unique @db.Uuid
  nhan_vien_id      String              @db.Uuid
  ban_giao_luc      DateTime            @default(now()) @db.Timestamptz(6)
  ghi_chu           String?
  trang_thai        String              @default("HOAN_THANH") @db.VarChar(50)
  chi_tiet_ban_giao chi_tiet_ban_giao[]
  don_thue          don_thue            @relation(fields: [don_thue_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  nguoi_dung        nguoi_dung          @relation(fields: [nhan_vien_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tep_don_thue      tep_don_thue[]

  @@index([don_thue_id], map: "idx_phieu_ban_giao_don")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model phieu_bao_tri {
  id              String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  thiet_bi_id     String          @db.Uuid
  don_thue_id     String?         @db.Uuid
  ly_do           String
  trang_thai      String          @default("DANG_XU_LY") @db.VarChar(50)
  ket_qua         String?
  nguoi_tao_id    String?         @db.Uuid
  bat_dau_luc     DateTime        @default(now()) @db.Timestamptz(6)
  hoan_thanh_luc  DateTime?       @db.Timestamptz(6)
  ghi_chu         String?
  don_thue        don_thue?       @relation(fields: [don_thue_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  nguoi_dung      nguoi_dung?     @relation(fields: [nguoi_tao_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  thiet_bi_vat_ly thiet_bi_vat_ly @relation(fields: [thiet_bi_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([thiet_bi_id], map: "idx_phieu_bao_tri_thiet_bi")
  @@index([trang_thai], map: "idx_phieu_bao_tri_trang_thai")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model phieu_tra_thiet_bi {
  id                    String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  don_thue_id           String                  @unique @db.Uuid
  nhan_vien_id          String                  @db.Uuid
  tra_luc               DateTime                @default(now()) @db.Timestamptz(6)
  ket_qua               String                  @default("HOP_LE") @db.VarChar(50)
  ket_qua_tien_coc      String?                 @db.VarChar(50)
  so_tien_hoan_coc      Decimal                 @default(0) @db.Decimal(12, 2)
  so_tien_khau_tru      Decimal                 @default(0) @db.Decimal(12, 2)
  ghi_chu               String?
  chi_tiet_tra_thiet_bi chi_tiet_tra_thiet_bi[]
  phi_phat_sinh         phi_phat_sinh[]
  don_thue              don_thue                @relation(fields: [don_thue_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  nguoi_dung            nguoi_dung              @relation(fields: [nhan_vien_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tep_don_thue          tep_don_thue[]

  @@index([don_thue_id], map: "idx_phieu_tra_thiet_bi_don")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model phu_kien {
  id                   String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ten_phu_kien         String                 @db.VarChar(255)
  mo_ta                String?
  tong_so_luong        Int                    @default(0)
  so_luong_bao_tri     Int                    @default(0)
  so_luong_mat         Int                    @default(0)
  vi_tri_kho_id        String?                @db.Uuid
  trang_thai           String                 @default("HOAT_DONG") @db.VarChar(50)
  ghi_chu              String?
  da_xoa_luc           DateTime?              @db.Timestamptz(6)
  created_at           DateTime               @default(now()) @db.Timestamptz(6)
  updated_at           DateTime               @default(now()) @db.Timestamptz(6)
  bo_di_kem            bo_di_kem[]
  lich_su_ton_kho      lich_su_ton_kho[]
  vi_tri_kho           vi_tri_kho?            @relation(fields: [vi_tri_kho_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phu_kien_gan_voi_don phu_kien_gan_voi_don[]

  @@index([vi_tri_kho_id], map: "idx_phu_kien_vi_tri")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model phu_kien_gan_voi_don {
  id                    String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  don_thue_id           String                  @db.Uuid
  chi_tiet_don_thue_id  String                  @db.Uuid
  bo_di_kem_id          String?                 @db.Uuid
  phu_kien_id           String                  @db.Uuid
  so_luong_can_giao     Int                     @default(0)
  so_luong_da_giao      Int                     @default(0)
  so_luong_da_tra       Int                     @default(0)
  trang_thai            String                  @default("DA_CHUAN_BI") @db.VarChar(50)
  ghi_chu               String?
  chi_tiet_ban_giao     chi_tiet_ban_giao[]
  chi_tiet_tra_thiet_bi chi_tiet_tra_thiet_bi[]
  bo_di_kem             bo_di_kem?              @relation(fields: [bo_di_kem_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  chi_tiet_don_thue     chi_tiet_don_thue       @relation(fields: [chi_tiet_don_thue_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  don_thue              don_thue                @relation(fields: [don_thue_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phu_kien              phu_kien                @relation(fields: [phu_kien_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([don_thue_id], map: "idx_phu_kien_gan_voi_don_don")
  @@index([phu_kien_id], map: "idx_phu_kien_gan_voi_don_phu_kien")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model tep_don_thue {
  id                 String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  don_thue_id        String              @db.Uuid
  phieu_ban_giao_id  String?             @db.Uuid
  phieu_tra_id       String?             @db.Uuid
  muc_dich           String              @db.VarChar(50)
  ten_file_goc       String              @db.VarChar(255)
  file_url           String
  loai_file          String?             @db.VarChar(50)
  kich_thuoc_file    Int?
  uploaded_by        String?             @db.Uuid
  uploaded_at        DateTime            @default(now()) @db.Timestamptz(6)
  don_thue           don_thue            @relation(fields: [don_thue_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phieu_ban_giao     phieu_ban_giao?     @relation(fields: [phieu_ban_giao_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phieu_tra_thiet_bi phieu_tra_thiet_bi? @relation(fields: [phieu_tra_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  nguoi_dung         nguoi_dung?         @relation(fields: [uploaded_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([don_thue_id], map: "idx_tep_don_thue_don")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model tep_hop_dong {
  id              String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  hop_dong_id     String        @db.Uuid
  ten_file_goc    String        @db.VarChar(255)
  file_url        String
  loai_file       String?       @db.VarChar(50)
  kich_thuoc_file Int?
  uploaded_by     String?       @db.Uuid
  uploaded_at     DateTime      @default(now()) @db.Timestamptz(6)
  hop_dong_giay   hop_dong_giay @relation(fields: [hop_dong_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  nguoi_dung      nguoi_dung?   @relation(fields: [uploaded_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([hop_dong_id], map: "idx_tep_hop_dong_hop_dong")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model thanh_toan {
  id                           String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  phien_thanh_toan_id          String?           @db.Uuid
  don_thue_id                  String?           @db.Uuid
  loai_thanh_toan              String            @db.VarChar(50)
  so_tien                      Decimal           @default(0) @db.Decimal(12, 2)
  phuong_thuc                  String            @default("THANH_TOAN_ONLINE") @db.VarChar(50)
  nha_cung_cap                 String?           @db.VarChar(50)
  trang_thai                   String            @default("CHO_THANH_TOAN") @db.VarChar(50)
  ma_phien_thanh_toan_provider String?           @unique(map: "idx_thanh_toan_provider_session_unique", where: raw("(ma_phien_thanh_toan_provider IS NOT NULL)")) @db.VarChar(255)
  ma_giao_dich_provider        String?           @db.VarChar(255)
  ma_su_kien_provider          String?           @unique(map: "idx_thanh_toan_provider_event_unique", where: raw("(ma_su_kien_provider IS NOT NULL)")) @db.VarChar(255)
  duong_dan_thanh_toan         String?
  du_lieu_provider             Json?
  ip_address                   String?           @db.VarChar(100)
  user_agent                   String?
  ghi_chu                      String?
  da_thanh_toan_luc            DateTime?         @db.Timestamptz(6)
  created_at                   DateTime          @default(now()) @db.Timestamptz(6)
  updated_at                   DateTime          @default(now()) @db.Timestamptz(6)
  don_thue                     don_thue?         @relation(fields: [don_thue_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_thanh_toan_don_thue")
  phien_thanh_toan             phien_thanh_toan? @relation(fields: [phien_thanh_toan_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([don_thue_id], map: "idx_thanh_toan_don")
  @@index([phien_thanh_toan_id], map: "idx_thanh_toan_phien")
  @@index([ma_phien_thanh_toan_provider], map: "idx_thanh_toan_provider_session")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model thiet_bi_gan_voi_don {
  id                    String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  don_thue_id           String                  @db.Uuid
  chi_tiet_don_thue_id  String                  @db.Uuid
  bo_di_kem_id          String?                 @db.Uuid
  thiet_bi_id           String                  @db.Uuid
  ma_tai_san_snapshot   String?                 @db.VarChar(100)
  so_serial_snapshot    String?                 @db.VarChar(255)
  tinh_trang_truoc      String?
  tinh_trang_sau        String?
  trang_thai            String                  @default("DA_CHUAN_BI") @db.VarChar(50)
  created_at            DateTime                @default(now()) @db.Timestamptz(6)
  chi_tiet_ban_giao     chi_tiet_ban_giao[]
  chi_tiet_tra_thiet_bi chi_tiet_tra_thiet_bi[]
  bo_di_kem             bo_di_kem?              @relation(fields: [bo_di_kem_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  chi_tiet_don_thue     chi_tiet_don_thue       @relation(fields: [chi_tiet_don_thue_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  don_thue              don_thue                @relation(fields: [don_thue_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  thiet_bi_vat_ly       thiet_bi_vat_ly         @relation(fields: [thiet_bi_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([don_thue_id], map: "idx_thiet_bi_gan_voi_don_don")
  @@index([thiet_bi_id], map: "idx_thiet_bi_gan_voi_don_thiet_bi")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model thiet_bi_vat_ly {
  id                                                        String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ma_tai_san                                                String                       @unique @db.VarChar(100)
  ten_tai_san                                               String                       @db.VarChar(255)
  mau_thiet_bi_id                                           String?                      @db.Uuid
  so_serial                                                 String?                      @unique @db.VarChar(255)
  ghi_chu_dinh_danh                                         String?
  vi_tri_mac_dinh_id                                        String?                      @db.Uuid
  vi_tri_hien_tai_id                                        String?                      @db.Uuid
  trang_thai                                                String                       @default("SAN_SANG") @db.VarChar(50)
  ghi_chu_tinh_trang                                        String?
  so_lan_cho_thue                                           Int                          @default(0)
  nguong_bao_tri                                            Int?
  da_xoa_luc                                                DateTime?                    @db.Timestamptz(6)
  created_at                                                DateTime                     @default(now()) @db.Timestamptz(6)
  updated_at                                                DateTime                     @default(now()) @db.Timestamptz(6)
  lich_su_di_chuyen_thiet_bi                                lich_su_di_chuyen_thiet_bi[]
  phieu_bao_tri                                             phieu_bao_tri[]
  thiet_bi_gan_voi_don                                      thiet_bi_gan_voi_don[]
  mau_thiet_bi                                              mau_thiet_bi?                @relation(fields: [mau_thiet_bi_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  vi_tri_kho_thiet_bi_vat_ly_vi_tri_hien_tai_idTovi_tri_kho vi_tri_kho?                  @relation("thiet_bi_vat_ly_vi_tri_hien_tai_idTovi_tri_kho", fields: [vi_tri_hien_tai_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  vi_tri_kho_thiet_bi_vat_ly_vi_tri_mac_dinh_idTovi_tri_kho vi_tri_kho?                  @relation("thiet_bi_vat_ly_vi_tri_mac_dinh_idTovi_tri_kho", fields: [vi_tri_mac_dinh_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([mau_thiet_bi_id], map: "idx_thiet_bi_vat_ly_mau")
  @@index([trang_thai], map: "idx_thiet_bi_vat_ly_trang_thai")
  @@index([vi_tri_hien_tai_id], map: "idx_thiet_bi_vat_ly_vi_tri")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model vi_tri_kho {
  id                                                                                String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  khu_vuc                                                                           String                       @db.VarChar(100)
  ke                                                                                String?                      @db.VarChar(50)
  tang                                                                              String?                      @db.VarChar(50)
  ngan                                                                              String?                      @db.VarChar(50)
  loai_vi_tri                                                                       String                       @default("KHO") @db.VarChar(50)
  trang_thai                                                                        String                       @default("HOAT_DONG") @db.VarChar(50)
  ghi_chu                                                                           String?
  created_at                                                                        DateTime                     @default(now()) @db.Timestamptz(6)
  lich_su_di_chuyen_thiet_bi_lich_su_di_chuyen_thiet_bi_vi_tri_sau_idTovi_tri_kho   lich_su_di_chuyen_thiet_bi[] @relation("lich_su_di_chuyen_thiet_bi_vi_tri_sau_idTovi_tri_kho")
  lich_su_di_chuyen_thiet_bi_lich_su_di_chuyen_thiet_bi_vi_tri_truoc_idTovi_tri_kho lich_su_di_chuyen_thiet_bi[] @relation("lich_su_di_chuyen_thiet_bi_vi_tri_truoc_idTovi_tri_kho")
  phu_kien                                                                          phu_kien[]
  thiet_bi_vat_ly_thiet_bi_vat_ly_vi_tri_hien_tai_idTovi_tri_kho                    thiet_bi_vat_ly[]            @relation("thiet_bi_vat_ly_vi_tri_hien_tai_idTovi_tri_kho")
  thiet_bi_vat_ly_thiet_bi_vat_ly_vi_tri_mac_dinh_idTovi_tri_kho                    thiet_bi_vat_ly[]            @relation("thiet_bi_vat_ly_vi_tri_mac_dinh_idTovi_tri_kho")
}

model xac_nhan_dieu_khoan {
  id                       String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  don_thue_id              String?            @db.Uuid
  khach_hang_id            String             @db.Uuid
  dieu_khoan_id            String             @db.Uuid
  dong_y_luc               DateTime           @default(now()) @db.Timestamptz(6)
  ip_address               String?            @db.VarChar(100)
  user_agent               String?
  ma_bam_noi_dung_snapshot String?            @db.VarChar(255)
  phien_thanh_toan         phien_thanh_toan[]
  don_thue                 don_thue?          @relation(fields: [don_thue_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_xac_nhan_dieu_khoan_don_thue")
  dieu_khoan_thue          dieu_khoan_thue    @relation(fields: [dieu_khoan_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  ho_so_khach_hang         ho_so_khach_hang   @relation(fields: [khach_hang_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  xac_thuc_otp             xac_thuc_otp[]

  @@index([don_thue_id], map: "idx_xac_nhan_dieu_khoan_don")
  @@index([khach_hang_id], map: "idx_xac_nhan_dieu_khoan_khach")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model xac_thuc_otp {
  id                     String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nguoi_dung_id          String               @db.Uuid
  xac_nhan_dieu_khoan_id String?              @db.Uuid
  otp_hash               String               @db.VarChar(255)
  muc_dich               String               @db.VarChar(50)
  trang_thai             String               @default("CHO_XAC_THUC") @db.VarChar(50)
  het_han_luc            DateTime             @db.Timestamptz(6)
  xac_thuc_luc           DateTime?            @db.Timestamptz(6)
  so_lan_sai             Int                  @default(0)
  created_at             DateTime             @default(now()) @db.Timestamptz(6)
  phien_thanh_toan       phien_thanh_toan[]
  nguoi_dung             nguoi_dung           @relation(fields: [nguoi_dung_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  xac_nhan_dieu_khoan    xac_nhan_dieu_khoan? @relation(fields: [xac_nhan_dieu_khoan_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([xac_nhan_dieu_khoan_id], map: "idx_xac_thuc_otp_dieu_khoan")
  @@index([nguoi_dung_id], map: "idx_xac_thuc_otp_nguoi_dung")
}
