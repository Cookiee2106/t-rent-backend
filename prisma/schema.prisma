generator client {
  provider        = "prisma-client-js"
  output          = "../node_modules/.prisma/client"
  previewFeatures = ["partialIndexes"]
}

datasource db {
  provider = "postgresql"
}

model ban_giao_vat_pham {
  id                    String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  chi_tiet_don_thue_id  String            @db.Uuid
  bo_di_kem_id          String?           @db.Uuid
  thiet_bi_id           String?           @db.Uuid
  phu_kien_id           String?           @db.Uuid
  ten_vat_pham_snapshot String            @db.VarChar(255)
  ma_tai_san_snapshot   String?           @db.VarChar(100)
  so_serial_snapshot    String?           @db.VarChar(255)
  so_luong_giao         Int               @default(1)
  ghi_chu_ban_giao      String?
  created_at            DateTime          @default(now()) @db.Timestamptz(6)
  bo_di_kem             bo_di_kem?        @relation(fields: [bo_di_kem_id], references: [id], onUpdate: NoAction)
  chi_tiet_don_thue     chi_tiet_don_thue @relation(fields: [chi_tiet_don_thue_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  phu_kien              phu_kien?         @relation(fields: [phu_kien_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  thiet_bi_vat_ly       thiet_bi_vat_ly?  @relation(fields: [thiet_bi_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([chi_tiet_don_thue_id, thiet_bi_id], map: "uq_ban_giao_vat_pham_chi_tiet_thiet_bi", where: raw("(thiet_bi_id IS NOT NULL)"))
  @@index([chi_tiet_don_thue_id], map: "idx_ban_giao_vat_pham_chi_tiet")
  @@index([phu_kien_id], map: "idx_ban_giao_vat_pham_phu_kien")
  @@index([thiet_bi_id], map: "idx_ban_giao_vat_pham_thiet_bi")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model bo_di_kem {
  id                                                         String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  mau_thiet_bi_chinh_id                                      String              @db.Uuid
  mau_thiet_bi_phu_id                                        String?             @db.Uuid
  phu_kien_id                                                String?             @db.Uuid
  so_luong                                                   Int                 @default(1)
  created_at                                                 DateTime            @default(now()) @db.Timestamptz(6)
  ban_giao_vat_pham                                          ban_giao_vat_pham[]
  mau_thiet_bi_bo_di_kem_mau_thiet_bi_chinh_idTomau_thiet_bi mau_thiet_bi        @relation("bo_di_kem_mau_thiet_bi_chinh_idTomau_thiet_bi", fields: [mau_thiet_bi_chinh_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  mau_thiet_bi_bo_di_kem_mau_thiet_bi_phu_idTomau_thiet_bi   mau_thiet_bi?       @relation("bo_di_kem_mau_thiet_bi_phu_idTomau_thiet_bi", fields: [mau_thiet_bi_phu_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  phu_kien                                                   phu_kien?           @relation(fields: [phu_kien_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([mau_thiet_bi_chinh_id, mau_thiet_bi_phu_id], map: "uq_bo_di_kem_mau_phu", where: raw("(mau_thiet_bi_phu_id IS NOT NULL)"))
  @@unique([mau_thiet_bi_chinh_id, phu_kien_id], map: "uq_bo_di_kem_phu_kien", where: raw("(phu_kien_id IS NOT NULL)"))
  @@index([mau_thiet_bi_chinh_id], map: "idx_bo_di_kem_mau_chinh")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model chi_tiet_don_thue {
  id                     String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  don_thue_id            String              @db.Uuid
  mau_thiet_bi_id        String              @db.Uuid
  so_luong               Int                 @default(1)
  gia_thue_ngay_snapshot BigInt              @default(0)
  tien_coc_snapshot      BigInt              @default(0)
  tien_thue              BigInt              @default(0)
  tien_coc               BigInt              @default(0)
  created_at             DateTime            @default(now()) @db.Timestamptz(6)
  updated_at             DateTime            @default(now()) @db.Timestamptz(6)
  ban_giao_vat_pham      ban_giao_vat_pham[]
  don_thue               don_thue            @relation(fields: [don_thue_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  mau_thiet_bi           mau_thiet_bi        @relation(fields: [mau_thiet_bi_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([don_thue_id], map: "idx_chi_tiet_don_thue_don")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model chi_tiet_gio_hang {
  id                     String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  gio_hang_id            String       @db.Uuid
  mau_thiet_bi_id        String       @db.Uuid
  so_luong               Int          @default(1)
  ngay_nhan              DateTime     @db.Timestamptz(6)
  ngay_tra               DateTime     @db.Timestamptz(6)
  gia_thue_ngay_snapshot BigInt       @default(0)
  tien_coc_snapshot      BigInt       @default(0)
  created_at             DateTime     @default(now()) @db.Timestamptz(6)
  updated_at             DateTime     @default(now()) @db.Timestamptz(6)
  gio_hang               gio_hang     @relation(fields: [gio_hang_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  mau_thiet_bi           mau_thiet_bi @relation(fields: [mau_thiet_bi_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([gio_hang_id], map: "idx_chi_tiet_gio_hang_gio")
  @@index([mau_thiet_bi_id], map: "idx_chi_tiet_gio_hang_mau")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model chi_tiet_phien_thanh_toan {
  id                     String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  phien_thanh_toan_id    String           @db.Uuid
  mau_thiet_bi_id        String           @db.Uuid
  so_luong               Int              @default(1)
  ngay_nhan              DateTime         @db.Timestamptz(6)
  ngay_tra               DateTime         @db.Timestamptz(6)
  gia_thue_ngay_snapshot BigInt           @default(0)
  tien_coc_snapshot      BigInt           @default(0)
  tien_thue              BigInt           @default(0)
  tien_coc               BigInt           @default(0)
  created_at             DateTime         @default(now()) @db.Timestamptz(6)
  mau_thiet_bi           mau_thiet_bi     @relation(fields: [mau_thiet_bi_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phien_thanh_toan       phien_thanh_toan @relation(fields: [phien_thanh_toan_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([mau_thiet_bi_id], map: "idx_ct_phien_thanh_toan_mau")
  @@index([phien_thanh_toan_id], map: "idx_ct_phien_thanh_toan_phien")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model danh_muc_he_thong {
  id                Int                 @id
  nhom_danh_muc     String              @db.VarChar(100)
  ma_danh_muc       String              @db.VarChar(100)
  ten_danh_muc      String              @db.VarChar(255)
  mo_ta             String?
  thu_tu            Int                 @default(1)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  danh_muc_thiet_bi danh_muc_thiet_bi[]
  tep_don_thue      tep_don_thue[]
  thanh_toan        thanh_toan[]

  @@unique([nhom_danh_muc, ma_danh_muc], map: "uq_danh_muc_he_thong_nhom_ma")
  @@index([nhom_danh_muc], map: "idx_danh_muc_he_thong_nhom")
}

model danh_muc_thiet_bi {
  id                String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ten_danh_muc      String            @unique @db.VarChar(255)
  tinh_chat_id      Int               @default(2502)
  da_xoa_luc        DateTime?         @db.Timestamptz(6)
  created_at        DateTime          @default(now()) @db.Timestamptz(6)
  updated_at        DateTime          @default(now()) @db.Timestamptz(6)
  danh_muc_he_thong danh_muc_he_thong @relation(fields: [tinh_chat_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  mau_thiet_bi      mau_thiet_bi[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model don_thue {
  id                                                String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ma_don                                            String              @unique @db.VarChar(100)
  khach_hang_id                                     String              @db.Uuid
  phien_thanh_toan_id                               String?             @unique @db.Uuid
  ngay_nhan                                         DateTime            @db.Timestamptz(6)
  ngay_tra                                          DateTime            @db.Timestamptz(6)
  so_ngay_thue                                      Int                 @default(1)
  tong_tien_thue                                    BigInt              @default(0)
  tong_tien_coc                                     BigInt              @default(0)
  trang_thai                                        Int                 @default(1102)
  huy_luc                                           DateTime?           @db.Timestamptz(6)
  ly_do_huy                                         String?
  ban_giao_luc                                      DateTime?           @db.Timestamptz(6)
  nguoi_ban_giao_id                                 String?             @db.Uuid
  ghi_chu_ban_giao                                  String?
  tra_luc                                           DateTime?           @db.Timestamptz(6)
  nguoi_nhan_tra_id                                 String?             @db.Uuid
  ghi_chu_thanh_ly                                  String?
  phi_phat_sinh_ly_do                               String?
  phi_phat_sinh_tien                                BigInt              @default(0)
  created_at                                        DateTime            @default(now()) @db.Timestamptz(6)
  updated_at                                        DateTime            @default(now()) @db.Timestamptz(6)
  chi_tiet_don_thue                                 chi_tiet_don_thue[]
  nguoi_dung_don_thue_khach_hang_idTonguoi_dung     nguoi_dung          @relation("don_thue_khach_hang_idTonguoi_dung", fields: [khach_hang_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  nguoi_dung_don_thue_nguoi_ban_giao_idTonguoi_dung nguoi_dung?         @relation("don_thue_nguoi_ban_giao_idTonguoi_dung", fields: [nguoi_ban_giao_id], references: [id], onUpdate: NoAction)
  nguoi_dung_don_thue_nguoi_nhan_tra_idTonguoi_dung nguoi_dung?         @relation("don_thue_nguoi_nhan_tra_idTonguoi_dung", fields: [nguoi_nhan_tra_id], references: [id], onUpdate: NoAction)
  phien_thanh_toan                                  phien_thanh_toan?   @relation(fields: [phien_thanh_toan_id], references: [id], onUpdate: NoAction)
  trang_thai_he_thong                               trang_thai_he_thong @relation(fields: [trang_thai], references: [id], onDelete: NoAction, onUpdate: NoAction)
  phieu_bao_tri                                     phieu_bao_tri[]
  tep_don_thue                                      tep_don_thue[]
  thanh_toan                                        thanh_toan[]

  @@index([khach_hang_id], map: "idx_don_thue_khach_hang")
  @@index([ngay_nhan, ngay_tra], map: "idx_don_thue_ngay")
  @@index([trang_thai], map: "idx_don_thue_trang_thai")
}

model gio_hang {
  id                String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  khach_hang_id     String              @unique @db.Uuid
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  updated_at        DateTime            @default(now()) @db.Timestamptz(6)
  chi_tiet_gio_hang chi_tiet_gio_hang[]
  nguoi_dung        nguoi_dung          @relation(fields: [khach_hang_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model ho_so_xac_minh {
  id                                                   String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  khach_hang_id                                        String              @db.Uuid
  so_cccd                                              String              @unique(map: "idx_ho_so_xac_minh_so_cccd_da_duyet", where: raw("(trang_thai = 203)")) @db.VarChar(50)
  anh_mat_truoc_url                                    String
  anh_mat_sau_url                                      String
  anh_cam_cccd_url                                     String
  trang_thai                                           Int                 @default(202)
  ly_do_tu_choi                                        String?
  nguoi_duyet_id                                       String?             @db.Uuid
  duyet_luc                                            DateTime?           @db.Timestamptz(6)
  created_at                                           DateTime            @default(now()) @db.Timestamptz(6)
  nguoi_dung_ho_so_xac_minh_khach_hang_idTonguoi_dung  nguoi_dung          @relation("ho_so_xac_minh_khach_hang_idTonguoi_dung", fields: [khach_hang_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  nguoi_dung_ho_so_xac_minh_nguoi_duyet_idTonguoi_dung nguoi_dung?         @relation("ho_so_xac_minh_nguoi_duyet_idTonguoi_dung", fields: [nguoi_duyet_id], references: [id], onUpdate: NoAction)
  trang_thai_he_thong                                  trang_thai_he_thong @relation(fields: [trang_thai], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([khach_hang_id], map: "idx_ho_so_xac_minh_khach_hang")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model mau_thiet_bi {
  id                                                      String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  danh_muc_id                                             String                      @db.Uuid
  ten_hang                                                String?                     @db.VarChar(255)
  ten_mau                                                 String                      @unique(map: "uq_mau_thiet_bi_ten") @db.VarChar(255)
  mo_ta                                                   String?
  anh_url                                                 String?
  gia_thue_ngay                                           BigInt                      @default(0)
  tien_coc                                                BigInt                      @default(0)
  da_xoa_luc                                              DateTime?                   @db.Timestamptz(6)
  created_at                                              DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at                                              DateTime                    @default(now()) @db.Timestamptz(6)
  trang_thai                                              Int                         @default(601)
  bo_di_kem_bo_di_kem_mau_thiet_bi_chinh_idTomau_thiet_bi bo_di_kem[]                 @relation("bo_di_kem_mau_thiet_bi_chinh_idTomau_thiet_bi")
  bo_di_kem_bo_di_kem_mau_thiet_bi_phu_idTomau_thiet_bi   bo_di_kem[]                 @relation("bo_di_kem_mau_thiet_bi_phu_idTomau_thiet_bi")
  chi_tiet_don_thue                                       chi_tiet_don_thue[]
  chi_tiet_gio_hang                                       chi_tiet_gio_hang[]
  chi_tiet_phien_thanh_toan                               chi_tiet_phien_thanh_toan[]
  trang_thai_he_thong                                     trang_thai_he_thong         @relation(fields: [trang_thai], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_mau_thiet_bi_trang_thai")
  danh_muc_thiet_bi                                       danh_muc_thiet_bi           @relation(fields: [danh_muc_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  thiet_bi_vat_ly                                         thiet_bi_vat_ly[]

  @@index([danh_muc_id], map: "idx_mau_thiet_bi_danh_muc")
  @@index([ten_hang], map: "idx_mau_thiet_bi_ten_hang")
  @@index([trang_thai], map: "idx_mau_thiet_bi_trang_thai")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model nguoi_dung {
  id                                                                      String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ho_ten                                                                  String               @db.VarChar(255)
  email                                                                   String               @unique @db.VarChar(255)
  so_dien_thoai                                                           String?              @unique @db.VarChar(20)
  mat_khau_hash                                                           String               @db.VarChar(255)
  vai_tro                                                                 String               @default("KHACH_HANG") @db.VarChar(50)
  trang_thai                                                              Int                  @default(101)
  dia_chi                                                                 String?
  trang_thai_xac_minh                                                     Int?
  da_xoa_luc                                                              DateTime?            @db.Timestamptz(6)
  created_at                                                              DateTime             @default(now()) @db.Timestamptz(6)
  updated_at                                                              DateTime             @default(now()) @db.Timestamptz(6)
  don_thue_don_thue_khach_hang_idTonguoi_dung                             don_thue[]           @relation("don_thue_khach_hang_idTonguoi_dung")
  don_thue_don_thue_nguoi_ban_giao_idTonguoi_dung                         don_thue[]           @relation("don_thue_nguoi_ban_giao_idTonguoi_dung")
  don_thue_don_thue_nguoi_nhan_tra_idTonguoi_dung                         don_thue[]           @relation("don_thue_nguoi_nhan_tra_idTonguoi_dung")
  gio_hang                                                                gio_hang?
  ho_so_xac_minh_ho_so_xac_minh_khach_hang_idTonguoi_dung                 ho_so_xac_minh[]     @relation("ho_so_xac_minh_khach_hang_idTonguoi_dung")
  ho_so_xac_minh_ho_so_xac_minh_nguoi_duyet_idTonguoi_dung                ho_so_xac_minh[]     @relation("ho_so_xac_minh_nguoi_duyet_idTonguoi_dung")
  trang_thai_he_thong_nguoi_dung_trang_thaiTotrang_thai_he_thong          trang_thai_he_thong  @relation("nguoi_dung_trang_thaiTotrang_thai_he_thong", fields: [trang_thai], references: [id], onDelete: NoAction, onUpdate: NoAction)
  trang_thai_he_thong_nguoi_dung_trang_thai_xac_minhTotrang_thai_he_thong trang_thai_he_thong? @relation("nguoi_dung_trang_thai_xac_minhTotrang_thai_he_thong", fields: [trang_thai_xac_minh], references: [id], onDelete: NoAction, onUpdate: NoAction)
  nhat_ky_thao_tac                                                        nhat_ky_thao_tac[]
  phien_thanh_toan                                                        phien_thanh_toan[]
  phieu_bao_tri                                                           phieu_bao_tri[]
  tep_don_thue                                                            tep_don_thue[]
  thanh_toan                                                              thanh_toan[]

  @@index([trang_thai_xac_minh], map: "idx_nguoi_dung_trang_thai_xac_minh")
  @@index([vai_tro], map: "idx_nguoi_dung_vai_tro")
}

model nhat_ky_thao_tac {
  id            String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nguoi_dung_id String?     @db.Uuid
  hanh_dong     String      @db.VarChar(100)
  doi_tuong     String      @db.VarChar(100)
  doi_tuong_id  String?     @db.Uuid
  noi_dung      String?
  du_lieu_truoc String?
  du_lieu_sau   String?
  ip_address    String?     @db.VarChar(100)
  created_at    DateTime    @default(now()) @db.Timestamptz(6)
  nguoi_dung    nguoi_dung? @relation(fields: [nguoi_dung_id], references: [id], onUpdate: NoAction)

  @@index([created_at], map: "idx_nhat_ky_thao_tac_created_at")
  @@index([nguoi_dung_id], map: "idx_nhat_ky_thao_tac_nguoi_dung")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model phien_thanh_toan {
  id                        String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  khach_hang_id             String                      @db.Uuid
  trang_thai                Int                         @default(901)
  tong_tien_coc             BigInt                      @default(0)
  tong_tien_thue            BigInt                      @default(0)
  ma_tham_chieu             String?                     @unique @db.VarChar(255)
  checkout_url              String?
  het_han_luc               DateTime                    @db.Timestamptz(6)
  da_thanh_toan_luc         DateTime?                   @db.Timestamptz(6)
  that_bai_luc              DateTime?                   @db.Timestamptz(6)
  created_at                DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at                DateTime                    @default(now()) @db.Timestamptz(6)
  chi_tiet_phien_thanh_toan chi_tiet_phien_thanh_toan[]
  don_thue                  don_thue?
  nguoi_dung                nguoi_dung                  @relation(fields: [khach_hang_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  trang_thai_he_thong       trang_thai_he_thong         @relation(fields: [trang_thai], references: [id], onDelete: NoAction, onUpdate: NoAction)
  thanh_toan                thanh_toan[]
  webhook_thanh_toan        webhook_thanh_toan[]

  @@index([khach_hang_id], map: "idx_phien_thanh_toan_khach_hang")
  @@index([trang_thai], map: "idx_phien_thanh_toan_trang_thai")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model phieu_bao_tri {
  id              String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  thiet_bi_id     String          @db.Uuid
  don_thue_id     String?         @db.Uuid
  ly_do           String
  ket_qua         String?
  nguoi_tao_id    String?         @db.Uuid
  bat_dau_luc     DateTime        @default(now()) @db.Timestamptz(6)
  hoan_thanh_luc  DateTime?       @db.Timestamptz(6)
  ghi_chu         String?
  don_thue        don_thue?       @relation(fields: [don_thue_id], references: [id], onUpdate: NoAction)
  nguoi_dung      nguoi_dung?     @relation(fields: [nguoi_tao_id], references: [id], onUpdate: NoAction)
  thiet_bi_vat_ly thiet_bi_vat_ly @relation(fields: [thiet_bi_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([don_thue_id], map: "idx_phieu_bao_tri_don")
  @@index([thiet_bi_id], map: "idx_phieu_bao_tri_thiet_bi")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model phu_kien {
  id                String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ten_phu_kien      String              @unique @db.VarChar(255)
  mo_ta             String?
  tong_so_luong     Int                 @default(0)
  vi_tri_luu_tru    String?             @db.VarChar(255)
  ghi_chu           String?
  da_xoa_luc        DateTime?           @db.Timestamptz(6)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  updated_at        DateTime            @default(now()) @db.Timestamptz(6)
  ten_hang          String?             @db.VarChar(255)
  ban_giao_vat_pham ban_giao_vat_pham[]
  bo_di_kem         bo_di_kem[]

  @@index([ten_hang], map: "idx_phu_kien_ten_hang")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model tep_don_thue {
  id                String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  don_thue_id       String            @db.Uuid
  ten_file_goc      String            @db.VarChar(255)
  file_url          String
  loai_file         String?           @db.VarChar(100)
  kich_thuoc_file   BigInt?
  uploaded_by       String?           @db.Uuid
  uploaded_at       DateTime          @default(now()) @db.Timestamptz(6)
  updated_at        DateTime          @default(now()) @db.Timestamptz(6)
  muc_dich_id       Int
  danh_muc_he_thong danh_muc_he_thong @relation(fields: [muc_dich_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_tep_don_thue_muc_dich")
  don_thue          don_thue          @relation(fields: [don_thue_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  nguoi_dung        nguoi_dung?       @relation(fields: [uploaded_by], references: [id], onUpdate: NoAction)

  @@index([don_thue_id], map: "idx_tep_don_thue_don")
  @@index([muc_dich_id], map: "idx_tep_don_thue_muc_dich_id")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model thanh_toan {
  id                  String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  don_thue_id         String            @db.Uuid
  phien_thanh_toan_id String?           @db.Uuid
  so_tien             BigInt            @default(0)
  loai_dong_tien_id   Int
  ma_giao_dich        String?           @unique(map: "idx_thanh_toan_ma_giao_dich_unique", where: raw("(ma_giao_dich IS NOT NULL)")) @db.VarChar(255)
  nguoi_thuc_hien_id  String?           @db.Uuid
  ghi_chu             String?
  created_at          DateTime          @default(now()) @db.Timestamptz(6)
  don_thue            don_thue          @relation(fields: [don_thue_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  danh_muc_he_thong   danh_muc_he_thong @relation(fields: [loai_dong_tien_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  nguoi_dung          nguoi_dung?       @relation(fields: [nguoi_thuc_hien_id], references: [id], onUpdate: NoAction)
  phien_thanh_toan    phien_thanh_toan? @relation(fields: [phien_thanh_toan_id], references: [id], onUpdate: NoAction)

  @@index([don_thue_id], map: "idx_thanh_toan_don")
  @@index([loai_dong_tien_id], map: "idx_thanh_toan_loai")
}

model thiet_bi_vat_ly {
  id                  String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  mau_thiet_bi_id     String              @db.Uuid
  ma_tai_san          String              @unique @db.VarChar(100)
  so_serial           String              @unique @db.VarChar(255)
  vi_tri_luu_tru      String?             @db.VarChar(255)
  trang_thai          Int                 @default(501)
  da_xoa_luc          DateTime?           @db.Timestamptz(6)
  created_at          DateTime            @default(now()) @db.Timestamptz(6)
  updated_at          DateTime            @default(now()) @db.Timestamptz(6)
  ban_giao_vat_pham   ban_giao_vat_pham[]
  phieu_bao_tri       phieu_bao_tri[]
  mau_thiet_bi        mau_thiet_bi        @relation(fields: [mau_thiet_bi_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  trang_thai_he_thong trang_thai_he_thong @relation(fields: [trang_thai], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([mau_thiet_bi_id], map: "idx_thiet_bi_vat_ly_mau")
  @@index([trang_thai], map: "idx_thiet_bi_vat_ly_trang_thai")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model trang_thai_he_thong {
  id                                                             Int                @id
  nhom_trang_thai                                                String             @db.VarChar(100)
  ma_trang_thai                                                  String             @db.VarChar(100)
  ten_trang_thai                                                 String             @db.VarChar(255)
  mo_ta                                                          String?
  thu_tu                                                         Int                @default(1)
  created_at                                                     DateTime           @default(now()) @db.Timestamptz(6)
  don_thue                                                       don_thue[]
  ho_so_xac_minh                                                 ho_so_xac_minh[]
  mau_thiet_bi                                                   mau_thiet_bi[]
  nguoi_dung_nguoi_dung_trang_thaiTotrang_thai_he_thong          nguoi_dung[]       @relation("nguoi_dung_trang_thaiTotrang_thai_he_thong")
  nguoi_dung_nguoi_dung_trang_thai_xac_minhTotrang_thai_he_thong nguoi_dung[]       @relation("nguoi_dung_trang_thai_xac_minhTotrang_thai_he_thong")
  phien_thanh_toan                                               phien_thanh_toan[]
  thiet_bi_vat_ly                                                thiet_bi_vat_ly[]

  @@unique([nhom_trang_thai, ma_trang_thai], map: "uq_trang_thai_he_thong_nhom_ma")
  @@index([nhom_trang_thai], map: "idx_trang_thai_he_thong_nhom")
}

model webhook_thanh_toan {
  id                  String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  id_su_kien          String            @unique @db.VarChar(255)
  phien_thanh_toan_id String?           @db.Uuid
  payload             String
  da_xu_ly            Boolean           @default(false)
  xu_ly_luc           DateTime?         @db.Timestamptz(6)
  loi_xu_ly           String?
  created_at          DateTime          @default(now()) @db.Timestamptz(6)
  phien_thanh_toan    phien_thanh_toan? @relation(fields: [phien_thanh_toan_id], references: [id], onUpdate: NoAction)

  @@index([phien_thanh_toan_id], map: "idx_webhook_thanh_toan_phien")
}
